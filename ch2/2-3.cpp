#include <iostream>
// #include <cstdint>
using namespace std;

/*
// 고정 너비 정수
int main() {
	std::int16_t i(5); // int16_t는 16bits == 2byte로 사용하겠다.
	std::int8_t myint = 65; // 1byte이므로 자동으로 char type
	cout << myint << endl;
	std::int_fast8_t fi(5); // 8bits 중에 가장 빠른 데이터 타입을 사용하겠다. signed char로 자동 변환
	std::int_least64_t fl(5); // 적어도 64bits를 사용하겠다. 8byte, long long 타입
	return 0;
}
*/

/*
// 무치형(void)
int main() {
	// void my_void;
	int i = 123;
	float f = 123.456f;
	void *my_void;
	my_void = (void*)&i;
	my_void = (void*)&f;
	return 0;
}
*/